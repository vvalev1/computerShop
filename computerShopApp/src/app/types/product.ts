export interface Product {
    _id: string
    productName: string,
    price: string,
    imageUrl: string,
    quantity: number, 
    productType: string,
    description: string,
    _ownerId: string
}